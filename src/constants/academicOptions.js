const GRADE_OPTIONS = Array.from({ length: 12 }, (_, index) => index.toString())
const GROUP_OPTIONS = Array.from({ length: 26 }, (_, index) => String.fromCharCode(65 + index))

export { GRADE_OPTIONS, GROUP_OPTIONS }
